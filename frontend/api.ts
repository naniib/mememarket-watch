// The application is configured to use a mock API for local development.
// This prevents "Failed to fetch" errors when the backend server is not running.
// The mock data is provided in `mockApi.ts`.
export * from './mockApi';
